source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-seo-tag"

# These gems were removed from stdlib in Ruby 3.4+ but required by jekyll-3.9 (github-pages)
if RUBY_VERSION >= "3.4"
  gem "csv"
  gem "bigdecimal"
  gem "ostruct"
  gem "logger"
end
